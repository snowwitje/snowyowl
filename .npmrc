# Hoist Storybook packages to root node_modules.
# Required so Vite can resolve @storybook/* from virtual modules
# (pnpm's strict isolation otherwise prevents cross-package resolution).
public-hoist-pattern[]=@storybook/*
public-hoist-pattern[]=storybook
