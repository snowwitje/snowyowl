// so-section emits no custom events — this file holds prop union types for
// consumers who want to reference them in TypeScript.

export type SectionGap = 'none' | 'xs' | 'sm' | 'md' | 'lg' | 'xl';
export type SectionHeadingLevel = 'h2' | 'h3' | 'h4';
